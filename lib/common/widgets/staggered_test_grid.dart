import 'package:flutter/material.dart';

class StaggeredTestGrid extends StatelessWidget {
  const StaggeredTestGrid({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}